# Write your MySQL query statement below
SELECT e.reports_to as employee_id, (SELECT name FROM Employees WHERE employee_id = e.reports_to) as name, COUNT(*) as reports_count, ROUND(AVG(e.age),0) as average_age FROM Employees as e WHERE e.reports_to IS NOT NULL GROUP BY e.reports_to ORDER BY e.reports_to;